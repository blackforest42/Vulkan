Dec 22

TODO: 
- remove 2 pipeline rendering, just use 1

VkDynamicState dynamicStates[] = {
    VK_DYNAMIC_STATE_VIEWPORT,
    VK_DYNAMIC_STATE_SCISSOR,
    VK_DYNAMIC_STATE_CULL_MODE // Required for dynamic culling
};

VkPipelineDynamicStateCreateInfo dynamicStateInfo{};
dynamicStateInfo.sType = VK_STRUCTURE_TYPE_PIPELINE_DYNAMIC_STATE_CREATE_INFO;
dynamicStateInfo.dynamicStateCount = 3;
dynamicStateInfo.pDynamicStates = dynamicStates;



- update cull mode during rendering
- write result to two separate textures

DONE:
- cube mesh aka "voxel" renderer done
- turned on validation layers default for debugging
- Working ray marcher w/ perlin noise to test
- Pivoted away from voxels
- finish cloud marching clouds tutorial
